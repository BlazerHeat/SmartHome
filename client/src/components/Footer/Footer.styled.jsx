import styled from "styled-components";

const StyledFooter = styled.footer`
    width: 100%;
    text-align: center;

    padding: 1rem;

    color: white;
    background: var(--primary-color);
`;

export { StyledFooter };
